### Advanced Generation Configuration Test
### Tests: All generation parameters and input/output saving

### Model Configuration
model_name_or_path: microsoft/DialoGPT-small
template: default

### Task Configuration
task: needle_haystack
task_dir: evaluation
save_dir: tests/results/advanced_generation
batch_size: 1
seed: 500

### Comprehensive Test Parameters
needle_context_lengths: [400, 800]
needle_depth_percents: [0, 25, 50, 75, 100]
needle_text: "The advanced generation code is GENCODE2024."
needle_question: "What is the advanced generation code?"
needle_haystack_data_source: custom
needle_save_inputs_outputs: true

### Full Generation Parameter Testing
needle_generation_temperature: 0.7      # High creativity
needle_generation_top_p: 0.85          # Nucleus sampling
needle_generation_top_k: 25            # Top-k sampling
needle_generation_max_tokens: 75       # Longer responses

### Performance Settings
infer_dtype: auto
use_cache: true
low_cpu_mem_usage: true