### Sequence Parallelism Test Configuration
### Tests: Different sequence parallel modes and GPU configurations

### Model Configuration
model_name_or_path: microsoft/DialoGPT-small
template: default
rope_scaling_type: linear
rope_scaling_factor: 2.0

### Sequence Parallelism Testing
sequence_parallel_size: 4
sequence_parallel_mode: llama3
flash_attn: fa2

### Inference Configuration
infer_dtype: bfloat16
use_cache: true
low_cpu_mem_usage: true

### Task Configuration
task: needle_haystack
task_dir: evaluation
save_dir: tests/results/sequence_parallel
batch_size: 1
seed: 600

### Parallel Processing Test Parameters
needle_context_lengths: [500, 1000, 1500]
needle_depth_percents: [0, 30, 70, 100]
needle_text: "The parallel processing key is PARALLEL2024."
needle_question: "What is the parallel processing key?"
needle_haystack_data_source: custom
needle_save_inputs_outputs: true

### Optimized Generation for Parallel Processing
needle_generation_temperature: 0.1
needle_generation_top_p: 0.9
needle_generation_top_k: 50
needle_generation_max_tokens: 35